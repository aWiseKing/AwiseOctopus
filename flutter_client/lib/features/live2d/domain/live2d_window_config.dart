import 'package:flutter/painting.dart';

class Live2dWindowConfig {
  Live2dWindowConfig._();

  static const Size defaultWindowSize = Size(400, 600);
  static const Size minWindowSize = Size(300, 400);

  static const double petScaleDefault = 1.0;
  static const double petScaleMin = 0.5;
  static const double petScaleMax = 2.0;
}
